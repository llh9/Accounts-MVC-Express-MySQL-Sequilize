{{#if loggedIn}}
<section class="video">
  <img src="/images/{{video.filename}}" alt="{{video.title}}">
  <div class="painting-details">
    <h3>{{video.title}}</h3>
    <p>By: Landon Hinkle</p>
    <p>Exhibition open until: Further Notice!</p>
  </div>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}